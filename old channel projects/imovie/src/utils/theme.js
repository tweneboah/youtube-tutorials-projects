import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#14213d",
    },
    secondary: {
      main: "#A22032",
    },
    common: {
      drawer: "#A22032",
    },
  },
});

export default theme;
